namespace: "sage"
name: "numpy-example"
description: "Numpy example which computes some stats of a test frame."
version: "0.1.0"
source:
  architectures:
    - "linux/amd64"
    # - "linux/arm64"
    # - "linux/arm/v7"
  url: "https://github.com/aabayomi/plugin-numpy-example.git"
  # url: "https://github.com/aabayomi/plugin-numpy-example"
  # url: "https://github.com/waggle-sensor/application-profiling"
  branch: "main" 


profiling:  # This is an example of how we would write YAML for the profiler
  # source:
  #   profiler_url: "https://github.com/waggle-sensor/application-profiling.git"  # required 
  #   profiler_branch: "tool-suite"  # optional, default: main
  #   profiler_directory: "plugin-simple"  # optional, default: root of git repository
  #   profiler_dockerfile: "Dockerfile"
  #   #app_name: "waggle/plugin-objectcounter:0.0.0"  s                      ## this is just an example the app name will be this definition
  #   profiler_app_docker_args: "-v /tmp/data-config.json:/run/waggle/data-config.json"   # Used this as defined from the profiler argument 
  #   profiler_app_args: "-stream top_live -object car -interval 1"       # this maybe combined with target but still unclear 
  targets:  # These are example run configurations (just arguments)
    - {args: "-interval 1 --use-gpu", timeout: 120}  # This is one config
    - {args: "--use-gpu", timeout: 60}  # This might be a more performance-intense config
    - {args: "--use-cpu", timeout: 0}  # This might be a config that only uses CPU and has no timeout (it relies on the app to close on its own)
  metrics: ['system', 'gpu-summary']  # This is where the developer specifies which external metrics to gather
  

resources:  # future feature
  - type: "RGB_image_producer"
    view: "top"
    min_resolution: "600x800"

# testing:
#     command: [python3 ,test.py]
    # entrypoint: [python3 ,test.py]


